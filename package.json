{
  "name": "origin-desktop",
  "productName": "Origin Desktop",
  "version": "1.0.0",
  "description": "Desktop Git client for Cursor Origin repositories",
  "author": "Origin Desktop",
  "license": "MIT",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"wait-on http://127.0.0.1:5173 && electron .\"",
    "build": "vite build",
    "start": "electron .",
    "pack": "npm run build && electron-packager . \"Origin Desktop\" --platform=win32 --arch=x64 --out=release --overwrite --icon=assets/icon.ico --ignore=node_modules --ignore=release --ignore=src --ignore=vite.config.js --win32metadata.CompanyName=\"Origin Desktop\" --win32metadata.ProductName=\"Origin Desktop\" --win32metadata.FileDescription=\"Desktop Git client for Cursor Origin\" --asar"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "concurrently": "^9.2.1",
    "electron": "^37.3.1",
    "electron-packager": "^17.1.2",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "vite": "^7.1.3",
    "wait-on": "^8.0.4"
  }
}
